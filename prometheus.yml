global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'fastapi-sentiment'
    scrape_interval: 5s
    static_configs:
      - targets: ['localhost:8000'] # Modificare con l'IP del container se avviato in reti separate
